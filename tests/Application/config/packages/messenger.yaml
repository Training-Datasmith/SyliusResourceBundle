framework:
    messenger:
        default_bus: command.bus
        buses:
            command.bus: ~
            query.bus: ~
        transports:
            sync: 'sync://'
        routing:
            'App\Shared\Application\Query\Query_Interface': 'sync'
            'App\Shared\Application\Command\Command_Interface': 'sync'
